{# Layout #}
{% extends '_layouts/site' %}

{# Blocks #}
{% block content %}
  <div class="bg-gray-100 py-8">
    <div class="max-w-4xl mx-auto space-y-6">
      {{ 'login' }}
    </div>
  </div>
{% endblock %}