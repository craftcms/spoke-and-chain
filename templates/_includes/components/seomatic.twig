{# SEOmatic component template to allow overiding of settings #}
{% set siteLinksSearchTarget = siteUrl(seomatic.site.siteLinksSearchTarget) %}
{% do seomatic.site.siteLinksSearchTarget(siteLinksSearchTarget) %}
